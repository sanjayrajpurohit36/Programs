               /*  Adds the new element at the starting of the array 
                   Returns the new length of array;

                */
var ary = [1,2,3,4,5];
var new_len = ary.unshift(0);
console.log("new len:",new_len);
console.log("Updated Array:",ary);